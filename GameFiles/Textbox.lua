function Textbox(text, x, y)
        local self = {}
        self.x = x
        self.y = y

        self.text = text

        function self.draw(self)
                
        end

        function self.update(self, dt)
                
        end

        return self
end